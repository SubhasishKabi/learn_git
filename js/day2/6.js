const meetingParticipants = [];

function addParticipant(name) {
  meetingParticipants.push(name);
  console.log("📋 Participants:", meetingParticipants);
}

addParticipant("Ravi");
addParticipant("Priya");